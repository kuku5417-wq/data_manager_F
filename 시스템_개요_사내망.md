# data_manager (사내망 전용) — 시스템 담당자 개요

조선소 시운전팀 **데이터 업로드 관리 앱**. Excel/DB/PDF/기상청 API → parquet 변환·병합 저장. `code_N` 앱군 **모든 parquet의 단일 생산자(Producer)** 이며, 소비앱(tbm/esg)은 이 parquet(및 MSSQL `jsh_*`)을 읽기만 한다.

> 이 폴더(`data_manager_F`)는 **사내망 전용 고정본**이다. 원본 data_manager의 사외망 자동감지·로컬경로·프록시 OFF·더미 폴백·Upstage/OpenAI LLM 폴백을 제거하고 사내망(NAS·프록시 ON·SOLA·MySQL/MSSQL)으로 고정했다.

---

## 1. 실행 / 환경

| 항목 | 값 |
|---|---|
| 프레임워크 | Streamlit (진입점 `app.py`) |
| 포트 | **8510** (`run.bat`, `uv run streamlit run app.py --server.port 8510`) |
| Python | **3.12** (`.python-version`, `requires-python >=3.12`) |
| 패키지 관리 | `uv` (`pyproject.toml` / `uv.lock`) |
| venv | 로컬 `C:` — `%LOCALAPPDATA%\venvs\data_manager` (네트워크 드라이브 금지) |
| 최초 설치 | `setup_env.bat` (venv 생성 + 의존성) |
| 가동 전 점검 | `python check_env.py` (`--quick` = 네트워크/DB 생략) |

**UI 구조**: 좌측 카테고리 내비 + 통합 카탈로그 표 + 상세 화면(`catalog_view.py`). 상세에서 **테이블별 "↻ 변환" 버튼**으로 해당 parquet 즉시 갱신. 기능 화면 4종: 데이터 변환 / parquet 추가 / 문서 파싱 / 안전메시지.

---

## 2. 데이터 스키마 (생산 parquet)

> **아래는 요약이다. 컬럼 단위 상세(타입·병합규칙·조인키·PII)는 [DATA_SCHEMA.md](DATA_SCHEMA.md) 참조.**
> 소비앱을 만들 때는 이 표가 아니라 정의서를 기준으로 할 것.

마스터 키 = **`PJT`** (호선번호, 예 `SN2601`). 저장 직전 `sn_util.ensure_sn` 로 SN 부착 통일. 저장 = `parquet_io.save_parquet_atomic`(임시파일→`os.replace` 원자적) + `touch_sentinel()`(소비앱 캐시 무효화).

### ESG 6종 — 엑셀 1파일(6시트) → parquet 6개 (`esg_converter.py`)
소스 `upload/esg/*.xlsx`. 병합 = **PJT 단위 교체**.

| parquet | 시트 | 주요 컬럼 |
|---|---|---|
| `trial_schedule` | 시운전일정 | PJT, 일정구분, 출항(dt), 복귀(dt), 대기 |
| `fuel_usage` | 사용량 | PJT, 일정구분, 공급처, Start/Finish(dt), HFO·LS_HFO·LS_MGO·RMA10·LDO·메탄올·LNG·LPG(float) |
| `fuel_price` | 단가 | PJT, 단가구분, HFO·LS_HFO·LS_MGO·RMA10·LDO·메탄올(float) |
| `lng_usage` | LNG | PJT, 구분, 작업구분, 용도, 비용/양/단가, Loading/Unloading(dt) |
| `fuel_plan` | 연간계획 | PJT, 인도년(int), 인도월(int), LNG·HFO·… (float, NaN→0) |
| `pjtmethod` | 공법 | PJT, 공법, 통합, 역, 선적, 하역, Speed, SG |

### 안전·작업허가
| parquet | 소스 | 주요 컬럼 |
|---|---|---|
| `ptwlist` | `upload/ptw/ptwlist_*.xlsx` (`tbm_converter.py`) | KYULGBN, PTW_AGBN, TEAMNM, DEPTNM, IOWKGBNNM, WKGBNNM, STDATE, EDDATE, PJT, AREA_DETAIL, WORK_NM, ACODENM, DATE(일별 확장) + risk_keywords/warning(LLM). **시운전팀만 필터**. dedup `[DEPTNM,PJT,AREA_DETAIL,ACODENM,DATE]` |
| `out` | `upload/out/outside_*.xlsx` (`out_converter.py`) | **name🔒, phone🔒**, company, visit_start/end(date), project, work_content, **greeter🔒**, dept |
| `ra` | out 파생(`derive_ra`) | out 컬럼 + period_start/end(최대7일 분할), is_commissioning, ra_done, ra_file, (소비앱 상태) excluded/exclude_reason/greeter_actual/manual. 3년 초과 제외 |

### 마스터·SQL (MySQL 소스 — `db_connector.py`)
| parquet | MySQL | 비고 |
|---|---|---|
| `pjtlist` | `shipinfo` | SHIPNUM ensure_sn, TYPEMODEL=ETHANE→SHIPTYPE=LPG. 컬럼: SHIPNUM,TITLE,TYPEMODEL,SHIPCLASS,DOCK,WORKFINISH,ISTRIAL,ISOUTSIDE,REGOWNER,PROJSEQ,SHIPTYPE,SEQNO,MAINHULLNUM (`SELECT *` 이라 DB 컬럼 추가 시 늘어남) |
| `milestone` | `pjtevnt` | **raw wide (unpivot 안 함)**, PJT→project. **project + 15개 이벤트 × PLAN/PROS/PERF = 46컬럼** (이벤트: LC·SP·BT·GT·CMR·COLDFROM/COLDTO·IE·MT·GASFROM/GASTO·STFROM/STTO·WF·DL) |
| `shipbbs` | `shipbbs` | `SELECT PJT,KIND,REMARK,INSERTDATE` (작성자 INSERTBY 미수집), project=ensure_sn |

### 자동수집 / 문서 / 메시지
| parquet | 소스 | 컬럼 |
|---|---|---|
| `weather` | 기상청 API허브 단기예보 (`api_weather.py`) | 거제시 장평동(nx90,ny69), 룰베이스 위험분석 |
| `date` | 정적 달력 (`date_manager.py`, holidays.KR) | 2025-01-01 ~ 오늘+7 |
| `mapping` | ACODENM→위험요소 LLM 캐시 (`ptw_enrich.py`) | work, keyword, warning |
| `message` | 직접 입력 (`message_store.py`) | date, team, content, ref_type, ref_path |
| `accident` | `upload/accident/*.pdf` + LLM (`doc_parser.py`) | id,seq,date,summary,cause,result,countermeasure,accident_type,keywords,text,source,pdf_filename |
| `guide` | `upload/guide/*.pdf` + LLM | id,standard_id,seq,title,text,keywords,pdf_filename,link |

🔒 = **개인정보**. 화면 표시는 `pii.mask_df_for_display`(→`mask_name`/`mask_phone`) 경유, parquet·MSSQL 원본은 위험성평가 실무상 원문 보관(하류 접근통제 대상). 담당자 실명(`HNAME`/`HSE_MANAGE`)·게시 작성자(`INSERTBY`)는 **생산 단계 미수집**(최소수집).

> 참고: `ptwlist_archive`는 상수(`ARCHIVE_DAYS=14`)만 있고 **생성 로직 미구현** — 카탈로그 노출 제외.

---

## 3. MSSQL 전송 (jsh_*) — 소비앱 공급

parquet 저장 완료 시 **저장된 parquet 파일을 다시 읽어** 운영서버 MSSQL `jsh_*` 테이블로 순차 전송(`parquet_io` → `db/auto_sync.push_saved`). 규칙: **테이블명 = `jsh_` + parquet stem**, `if_exists="replace"`(전체 교체·idempotent). `.env DB_*` 미설정 시 휴면, 실패해도 parquet 저장 무영향(best-effort).

전송 대상 16종 (`db/tables.py`): `ptwlist, out, ra, mapping, message, trial_schedule, fuel_usage, fuel_price, lng_usage, fuel_plan, pjtmethod, weather, date, milestone, pjtlist, shipbbs` → `jsh_*`.

소비: **tbm / esg_mssql** 이 `jsh_*` 를 `SELECT *` 로 읽음(MSSQL 우선, parquet 폴백).
추가로 **tbm 은 RA 상태(jsh_ra)를 되쓴다** — 저장 시 트랜잭션 내 전량 교체(DROP 없음).

---

## 4. DB 연결

### 소스 — MySQL (읽기)
- 라이브러리: SQLAlchemy + **pymysql**. URL `mysql+pymysql://user:pw@host:port/db?charset=utf8mb4` (`app_config.db_url()`), QueuePool + pre_ping.
- 대상: `shipinfo`, `pjtevnt`, `shipbbs`.
- env: `DB_MYSQL_HOST / DB_MYSQL_PORT(3306) / DB_MYSQL_DATABASE / DB_MYSQL_USER / DB_MYSQL_PASSWORD`.
- HOST 미설정/`YOUR_` 시작 → parquet 폴백(무중단).

### 타겟 — MSSQL 운영서버 (jsh_* 쓰기)
- 라이브러리: SQLAlchemy + **pyodbc**, `mssql+pyodbc:///?odbc_connect=...`, **ODBC Driver 18** (`TrustServerCertificate=yes`).
- env(사용자 지정, 오타 `DB_SEVER` 그대로 + `DB_SERVER` 폴백): `DB_SEVER / DB_PORT(1433) / DB_NAME / DB_USERNAME / DB_PASSWORD / DB_DRIVER`.
- 미설정 시 전체 휴면, pyodbc lazy import.

> 키 읽기 경로 차이: MySQL 키 = `app_config`(os.getenv/dotenv), MSSQL 키 = `path_config.read_secret`(.env 직접 파싱).

---

## 5. 사용 라이브러리 (`pyproject.toml`)

| 용도 | 패키지 |
|---|---|
| 코어 | streamlit≥1.35, pandas≥2.0, pyarrow≥14, openpyxl≥3.1 |
| 기능 | requests(날씨), holidays(달력), pdfplumber(PDF), openai(LLM), python-dotenv, watchdog(ptw 워처) |
| DB | sqlalchemy≥2.0, pymysql(MySQL 소스), **pyodbc**(MSSQL 전송, win32) |
| Windows | **pywin32**(DRM 엑셀 win32 COM) |

시스템 요구: 64비트 Python 3.12, **ODBC Driver 18 for SQL Server** 설치(MSSQL 전송용). 사내망 uv 설정: `UV_NATIVE_TLS=true`, `UV_CONCURRENT_DOWNLOADS=1`, `UV_HTTP_TIMEOUT=300`.

---

## 6. 모듈 구조

| 파일 | 역할 |
|---|---|
| `app.py` | 메인 진입점 — 기능 화면 렌더 함수 + `catalog_view.render_app` 라우터 |
| `catalog_view.py` | 재디자인 UI(내비+카탈로그+상세), 기능화면 handlers 위임 |
| `table_actions.py` | 테이블 단위 변환 레지스트리 `RUN_SINGLE` + `run(key)`, 폴더 스캔 |
| `app_config.py` | **사내망 고정** 환경설정(경로/DB/LLM/OCR/프록시/날씨) |
| `settings.py` | `AppSecrets` — app_config를 typed property(`secrets.*`)로 노출 |
| `path_config.py` | 사내망 NAS 경로/업로드/백업/센티널 |
| `esg_converter.py` / `tbm_converter.py` / `out_converter.py` | ESG 6시트 / ptwlist 일별확장 / out+ra 변환 |
| `db_connector.py` | MySQL → pjtlist/milestone/shipbbs, PII 컬럼 제거, DB 장애 시 기존 parquet 폴백 |
| `parquet_io.py` | `save_parquet_atomic` + MSSQL 자동전송 훅 |
| `db/` | MSSQL 전송 — `connection`(엔진)/`tables`(화이트리스트)/`sync`(전체교체)/`auto_sync`(순차 자동 push) |
| `llm_client.py` | **사내 SOLA 단일** LLM 호출(프록시 우회, verify=False) |
| `api_weather.py` / `doc_parser.py` / `date_manager.py` | 기상청 / PDF(pdfplumber+LLM) / 달력 |
| `ptw_enrich.py` | ptwlist 위험키워드(mapping + 미매핑 LLM) |
| `pii.py` | 개인정보 마스킹(표시 전용) `mask_name`/`mask_phone`/`mask_df_for_display` |
| `sn_util.py` / `datetime_util.py` / `drm_reader.py` | SN 부착 / 날짜 파싱 / DRM 엑셀 win32 복원 |
| `message_store.py` / `job_status.py` | message CRUD / 무인 잡 상태 기록 |
| `ui_components.py` / `ui_styles.py` | 신호등 diff·카드 / 산업형 CSS |
| `check_env.py` | 가동 전 점검(doctor) |

---

## 7. 무인 스케줄러 잡

| 잡 | 권장 주기 | 역할 |
|---|---|---|
| `import_job.py` | 30분 | `upload/{esg,out}` 신규 변환 → 성공분 `_processed/`, 실패 `_failed/`(멱등). ptw 제외(워처) |
| `ptw_watch_job.py` | 로그온 시 상시 | watchdog로 `upload/ptw` 실시간 감지→변환(미설치 시 폴링 폴백) |
| `doc_job.py` | 일 1회 | `upload/{accident,guide}` PDF **증분** 파싱 |
| `db_job.py` | 일 1회 (07:30) | MySQL → pjtlist/milestone/shipbbs 재생성 |
| `date_job.py` | 일 1회 (00:30) | date.parquet 갱신 |
| `weather_job.py` | 일 2회 (06:30, 12:00) | 기상청 → weather.parquet |
| `ra_job.py` | 선택 | out → ra 파생(regenerate_ra) |

공통: 헤드리스·원자적 저장·`0/1` 종료·`parquet/_jobstatus/<job>.json` 기록 → UI 모니터링.

---

## 8. 환경변수 / 시크릿 키 (사내망 — 값 아닌 키 이름만)

시크릿 파일: 상위 폴더 `secret/.env` (저장소 외부, **커밋 금지**). 탐색: `SHI_ENV_FILE` → 상위 폴더 자동 탐색. 템플릿 `.env.template`.

| 분류 | 키 |
|---|---|
| 경로(NAS) | `NAS_BASE_PATH`(필수, 구 `NAS_PATH` 폴백), `NAS_UPLOAD_PATH`, `NAS_PARQUET_PATH`, `NAS_DUMMY_PATH` |
| MySQL 소스 | `DB_MYSQL_HOST/PORT/DATABASE/USER/PASSWORD` |
| MSSQL 타겟 | `DB_SEVER`(+`DB_SERVER`)`/DB_PORT/DB_NAME/DB_USERNAME/DB_PASSWORD/DB_DRIVER` |
| LLM(SOLA) | `LLM_SOLAR_API_KEY/LLM_SOLAR_API_URL/LLM_SOLAR_MODEL` |
| OCR(SOLA/DoXA) | `OCR_SOLAR_API_KEY/OCR_SOLAR_API_URL/OCR_SOLAR_MODEL`, `OCR_DOXA_API_KEY/OCR_DOXA_API_URL` |
| 프록시 | `HTTP_PROXY`, `HTTPS_PROXY` (사내 `http://60.200.254.1:9090`) |
| 기상청 | `WEATHER_API_KEY`, `WEATHER_BASE_URL` |

---

## 9. 데이터 흐름

```
[소스]                          [변환/생성]              [저장]                 [전송]           [소비]
upload/esg/*.xlsx          → esg_converter        ┐
upload/ptw/ptwlist_*       → tbm_converter        │
upload/out/outside_*       → out_converter        │
MySQL shipinfo/pjtevnt/    → db_connector         ├→ save_parquet_atomic → auto_sync      → tbm
     shipbbs                                       │   (원자적)             MSSQL jsh_*      esg_mssql
기상청 API                 → api_weather          │   parquet/*.parquet   (전체교체,       (jsh_* 읽기)
upload/{accident,guide}/pdf → doc_parser(LLM)      │   + touch_sentinel     best-effort)
ACODENM                    → ptw_enrich → mapping  │
정적 달력 / 직접 입력       → date_manager/message ┘
```

---

## 10. 사내망 전용 설정 요약

- **망**: `IS_INTERNAL=True` 고정(자동감지 없음). `NAS_BASE_PATH` 미설정 시 기동 거부.
- **경로**: NAS(`NAS_BASE_PATH\{parquet,upload,sql}`)만 사용(로컬 `F:\code\data` 폴백 제거).
- **프록시**: 외부 인터넷 호출(기상청·Upstage 등)만 회사 프록시 경유, 사내 서버(SOLA/DoXA/MySQL/MSSQL)는 직접. `SSL_VERIFY = not USE_PROXY`(프록시 경유 시 False).
- **LLM**: 사내 **SOLA 단일**(Upstage/OpenAI 폴백 제거). Qwen3 `<think>` 블록 정규식 제거, `max_token` 단수, 프록시 우회.
- **DB 폴백**: DB 미연결 시 기존 parquet 유지(무중단). 더미 폴백은 제거.
- **제거된 파일**: `create_dummy.py`(더미 생성), `diag_llm.py`/`diag_ptw.py`/`run_diag.bat`(개발·진단).

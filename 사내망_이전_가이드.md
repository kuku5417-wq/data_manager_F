# 사내망 이전 가이드 — zip 다운로드 후 설치

> 3앱: **data_manager**(생산) / **tbm**·**esg**(소비).
> 공용 설정 `app_config.py`는 **각 repo 루트에 동봉**됨. 시크릿은 단일 `secret/.env`.
> 작성: 2026-06-11

---

## 0. 핵심 요약
- 각 앱은 **자기 repo의 `app_config.py`** 를 사용 → `secret/.env`(SECTION_KEY)에서 경로·키·프록시를 읽음.
- `app_config`는 자기 위치에서 **위로 올라가며 `secret/.env`** 를 찾는다(환경변수 `SHI_ENV_FILE`로 강제 지정 가능). 임의 경로 압축해제 OK.
- ⚠️ **네트워크 드라이브에 두면 numpy ImportError** → 반드시 **로컬 디스크(C:)에 venv** 생성.
- 사내망(NAS 접근 가능) 자동 감지: `NAS_BASE_PATH` 존재 시 사내망 모드(프록시·DB 활성).

## 1. 폴더 구조 (권장)
```
<부모>\                        예) C:\work  또는  현행 F:\code
├── data_manager\              (app_config.py 동봉)
├── tbm\             (app_config.py 동봉)
├── esg\                (app_config.py 동봉)
├── secret\.env                ← 직접 작성 (2번). zip엔 없음(보안)
└── data\                      ← 공유 데이터 (parquet/, upload/, sql/)
```
- 각 zip을 같은 `<부모>` 아래에 풀면 됨. `app_config`가 `<부모>\secret\.env`를 자동 탐색.

## 2. `secret\.env` 작성 (zip에 없음 — 필수)
- 아무 앱의 **`.env.template`** 를 복사 → `<부모>\secret\.env` 로 저장 후 사내망 실값 입력:
  - `NAS_BASE_PATH` (실제 NAS UNC, 예 `\\nas\...\06_Commissioning_TEAM`),
    `NAS_PARQUET_PATH`, `NAS_UPLOAD_PATH`, `NAS_DUMMY_PATH`
  - `DB_MYSQL_HOST/PORT/DATABASE/USER/PASSWORD`
  - `LLM_SOLAR_API_KEY`, `LLM_SOLAR_API_URL` (사내 LLM)
  - `HTTP_PROXY`, `HTTPS_PROXY` (사내 프록시)
  - `WEATHER_API_KEY`
  - (사외망 테스트 시 `DATA_PATH=<부모>\data`)
- `.env` 를 다른 위치에 두려면 실행 환경에 `SHI_ENV_FILE=경로\.env` 설정.

## 3. 로컬 venv / 의존성 설치 (필수)

> ⚠️ **사내망은 `UV_NATIVE_TLS=true`(=Windows 인증서 저장소 사용) 필수.** 없으면
> `invalid peer certificate: UnknownIssuer` (사내 SSL 검사 인증서 미신뢰)로 다운로드 실패.
> (uv에 `--system-certs` 옵션은 **없음** — 환경변수 `UV_NATIVE_TLS` 또는 플래그 `--native-tls` 사용.)

**(A) pyproject 앱**(data_manager·esg) — `uv sync`:
```powershell
$env:HTTP_PROXY = "http://<사내프록시>:포트"; $env:HTTPS_PROXY = $env:HTTP_PROXY
$env:UV_PROJECT_ENVIRONMENT = "C:\venvs\<app>"     # 로컬 venv(네트워크 드라이브 numpy 회피)
$env:UV_NATIVE_TLS = "true"                         # ← 없으면 UnknownIssuer
uv sync
```
**(B) requirements.txt 앱**(tbm):
```powershell
$env:UV_NATIVE_TLS = "true"
uv venv C:\venvs\tbm --python 3.12
$env:UV_PROJECT_ENVIRONMENT = "C:\venvs\tbm"
uv pip install -r requirements.txt
```
- `python-dotenv`는 **app_config 필수** — 3앱 의존성(pyproject/requirements.txt)에 포함됨.
- **tbm은 streamlit 1.35.x** 유지(상위 버전은 `st.radio` 오류). esg/data_manager는 최신 가능.

### 3-1. `UnknownIssuer` / 다운로드 실패 트러블슈팅
1. **1순위**: `$env:UV_NATIVE_TLS = "true"` 설정 후 `uv sync`(Windows 인증서 저장소의 사내 CA 사용). 플래그로도 가능: `uv sync --native-tls`.
   - ⚠️ `--system-certs`는 uv에 **없는 옵션** → `unexpected argument '--system-certs' found` 에러. `--native-tls`/`UV_NATIVE_TLS` 사용.
2. 그래도 실패 시: 사내 root CA `.pem` 확보 → `$env:SSL_CERT_FILE = "C:\path\corp-ca.pem"`.
3. 프록시 미설정이 원인이면: `HTTP_PROXY`/`HTTPS_PROXY` 환경변수 설정.
4. **최후수단**(보안 비권장): `uv sync --native-tls --allow-insecure-host pypi.org --allow-insecure-host files.pythonhosted.org`.

## 4. 실행 (반드시 로컬 venv python으로)
```powershell
& C:\venvs\data_manager\Scripts\python.exe -m streamlit run data_manager\app.py   --server.port 8510   # 생산
& C:\venvs\tbm\Scripts\python.exe -m streamlit run tbm\app2_tbm.py --server.port 8502   # 단일 통합앱(4메뉴)
& C:\venvs\esg\Scripts\python.exe -m streamlit run esg\app.py        --server.port 8503
```

## 5. 데이터 생산 (사내망 — data_manager만)
- data_manager가 단일 생산자: DB→parquet, 엑셀→parquet, PDF/LLM/날씨. 소비 앱은 읽기 전용.
- 무인 잡(작업 스케줄러, 로컬 venv python): `db_job.py`·`date_job.py`·`weather_job.py`·`import_job.py`·`doc_job.py`.
- 소비 앱(tbm/esg)은 `NAS_PARQUET_PATH`(공유 parquet) 읽음 + data2 더미 폴백.

## 6. 점검
```powershell
& C:\venvs\<app>\Scripts\python.exe <app>\app_config.py
```
→ `환경: 사내망`, `BASE_PATH`(NAS), `USE_PROXY: True`, `db_url set: True` 면 정상.
사외망이면 `사외망`/`USE_PROXY: False`/로컬 경로.

## 7. 주의 — app_config 동기화
3 repo에 동봉된 `app_config.py`는 **동일본**이어야 한다. 로직 수정 시 3곳 모두 갱신(data_manager 기준 복사 권장).

# 운영서버(MSSQL) 전환 체크리스트 (사내망 전용)

> data_manager = parquet 생산 + **MSSQL push**(공유). 소비앱 = **`tbm` · `esg_mssql`**(MSSQL 읽기).
> `tbm` 은 추가로 **RA 상태(`jsh_ra`)를 되쓴다** — 카드 저장 시 트랜잭션 내 전량 교체(DROP 없음).
> `ocr`은 데이터 테이블을 읽지 않으므로 대상 아님. 전 구간은 로컬 SQLite 라운드트립으로 **검증 완료**(아래 참조).
> **무중단 설계**: `.env DB_*` 미설정/연결 실패 시 모든 앱이 기존 parquet 동작으로 폴백.

## 전제
- 사내망, 운영 MSSQL 서버 접속 가능
- 각 PC에 **ODBC Driver 18 for SQL Server**(`msodbcsql18`) 설치 (시스템 설치 — venv와 별개)

## 1) 시크릿 입력 — `F:\code\secret\.env` (커밋 금지)
```
DB_SEVER=<운영서버 호스트>
DB_PORT=<포트, 기본 1433>
DB_NAME=<DB명>
DB_USERNAME=<계정>
DB_PASSWORD=<비밀번호>
# 선택: DB_DRIVER=ODBC Driver 18 for SQL Server  (기본값과 동일하면 생략)
```
> 플레이스홀더(`0.0.0.0`/`0000`/빈값)면 `is_configured()=False` → 휴면(parquet). 실제값 입력 시 활성.

## 2) data_manager — parquet → MSSQL 전송 (생산자, 1회/갱신 시)
1. `setup_env.bat` 실행 → `uv sync`가 갱신된 `pyproject`로 **pyodbc 설치**.
2. 앱 실행 → **원본 업로드 변환/상태** 화면 → **🛢 운영서버(MSSQL) 전송** 펼치기.
3. **⬆️ 전체 테이블 → MSSQL 전송 (jsh_*)** 클릭 → `jsh_*` 16개 테이블 전체 교체(idempotent).
   - 대상: ptwlist, out, ra, mapping, message, trial_schedule, fuel_usage, fuel_price,
     lng_usage, fuel_plan, pjtmethod, weather, date, milestone, pjtlist, shipbbs
   - parquet 생산은 그대로 유지(비파괴) — 기존 parquet 소비앱도 계속 동작.

## 3) 소비앱 — `tbm` · `esg_mssql` (사내망 clone)
| 앱 | repo | venv | 포트 |
|----|------|------|------|
| esg_mssql | github.com/kuku5417-wq/esg_mssql (private) | esg_mssql | 8513 |
| tbm | github.com/kuku5417-wq/tbm (private) | tbm | 8502 |

각 repo: `setup_env.bat`(uv sync → pyodbc) → `run.bat`. `.env DB_*` 설정돼 있으면 **자동으로 `jsh_*` 읽기**.

## 데이터 읽기 전환 지점 (참고)
- **esg_mssql**: `modules/data_processor.py` 8개 raw 로더 → `db/reader.read_table(key)` (MSSQL `jsh_<key>` → parquet 폴백)
- **tbm**: `modules/parquet_manager.load_parquet` + `modules/db_connector._load_parquet` 상단에
  `db/reader.read_mssql(key)` 주입 (None이면 기존 parquet/data2 폴백)
- 테이블명 규칙: **`jsh_<parquet stem>` 소문자** (`db/tables.table_of`)

## 검증 현황 (로컬 SQLite 라운드트립, 실서버 전)
- ✅ **push** 16/16 테이블 parquet → `jsh_*` (data_manager)
- ✅ **esg_mssql read** 8테이블 + 다운스트림(`get_ship_summary` 40×32, 배출량 합 15,758 tCO₂e)
- ✅ **tbm read** 9테이블 + 중앙리더(`load_parquet`) + `db_connector.fetch_pjtlist/milestone/shipbbs`
- ✅ **tbm write** `jsh_ra` 트랜잭션 전량 교체(`db/sync.push_ra`) — 미설정 시 휴면, 실패해도 parquet 저장은 유효
- ⏳ **실서버에서 남는 것**: 실제 호스트 접속(ODBC Driver 18 + pyodbc) — SQL 로직은 위에서 검증됨

## 롤백
- `.env`의 `DB_*`를 비우거나 플레이스홀더로 되돌리면 즉시 parquet 동작으로 복귀(코드 변경 불필요).
- 기존 `esg_260605` · `tbm_system_v6`는 parquet 버전으로 별도 유지.

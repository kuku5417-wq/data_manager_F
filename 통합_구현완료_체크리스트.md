# 데이터 통합 구현 완료 — 체크리스트 & 검증 가이드

> 단일 소스 `F:\code\data\parquet`. data_manager 생산 / tbm·ESG 소비.
> 코드 변경 완료분과 운영자 실행 단계, 검증 방법 정리. 작성: 2026-06-08

---

## A. 완료된 코드 변경

### data_manager (생산)
| 파일 | 내용 |
|---|---|
| `sn_util.py` (신규) | `ensure_sn` — 호선 SN 부착 통일 |
| `parquet_io.py` (신규) | `save_parquet_atomic` — 임시파일→rename 원자적 저장 |
| `settings.py` (신규) | `.env` SECTION_KEY → AppSecrets(proxies/ssl/db_url/sola/weather) |
| `migrate_sn.py` (신규) | 기존 parquet 호선 SN 일괄 통일 (0단계 1회 실행) |
| `llm_client.py` (신규) | SOLA→OpenAI 폴백 (tbm 이식) |
| `api_weather.py` (신규) | 기상청 수집 (tbm 이식, 자립형) |
| `doc_parser.py` (신규) | PDF→accident/guide.parquet (tbm 이식) |
| `date_manager.py` (신규) | date.parquet 생성 (tbm 이식) |
| `db_connector.py` (신규) | shipinfo/pjtevnt/shipbbs→parquet. **milestone raw wide(unpivot 안 함)**, ensure_sn |
| `ptw_enrich.py` (신규) | ptwlist risk_keywords/warning. 미매핑 LLM, 실패내성, 빈값 캐시금지, mapping 재읽기-병합 |
| `message_store.py` (신규) | message.parquet CRUD |
| `weather_job.py` (신규) | 무인 날씨수집 (스케줄러용) |
| `path_config.py` | NAS_PATH=06_Commissioning_TEAM 포함, UPLOAD_SECTIONS에 accident/guide 추가, get_backup_dir |
| `esg/tbm/out_converter.py` | 저장 직전 ensure_sn + 원자적 저장. tbm은 팀필터(시운전팀)+ptw_enrich |
| `app.py` | ESG/TBM/Out 저장·미리보기 ensure_sn. 신규 탭 5종(날씨/문서/매핑/메시지/DB·날짜) |

### tbm_system_v6 (소비)
| 파일 | 내용 |
|---|---|
| `config/nas_config.py` | parquet 읽기 base/`parquet`/, base=NAS_PATH→LOCAL_DATA1, RAW=upload/ |
| `config/settings.py` | `.env` SECTION_KEY 읽기, NAS_PATH/LOCAL_DATA1·2 프로퍼티 |
| `modules/ptw_processor.py` | `_normalize_project`→ensure SN, `_adapt_dm_ptwlist`(DM스키마→tbm) + load_ptw_parquet 적용 |
| `modules/db_connector.py` | `_normalize_hullno`→ensure SN, milestone parquet 폴백 시 read-unpivot(wide→long) |
| `modules/parquet_manager.py` | `check_sentinel_and_clear`/`refresh_button` |
| `app1/app2/app3` | 진입부 센티널 체크 + 사이드바 새로고침 버튼 |
| `views/tbm/*, views/ra/*, views/admin/ptw_view` | 표시단 `SN{project}`→`{project}` (SN 중복 제거) |

### esg_260605 (소비)
| 파일 | 내용 |
|---|---|
| `config/nas_config.py` | `_nas_base` 감지를 `base/parquet` 기준으로(통합 flat 구조 인식) |

---

## B. 운영자 실행 단계 (순서 중요)

1. **`.env` 작성**: `F:\code\secret\.env` 에 `data_manager/.env.template` 키 채우기
   (`NAS_BASE_PATH`, `DATA_PATH=F:\code\data`, `DB_MYSQL_*`, `LLM_SOLAR_*`/`LLM_UPSTAGE_*`,
   `HTTP_PROXY`/`HTTPS_PROXY`, `WEATHER_API_KEY`). 3앱 공용 `F:\code\app_config.py`가 이 키를 읽음.
2. **의존성 설치** (data_manager): `holidays`, `pdfplumber`, `openai`, `sqlalchemy`, `pymysql` 추가 필요
   → `uv add holidays pdfplumber openai sqlalchemy pymysql`
3. **마이그레이션 1회**: `cd F:\code\data_manager && uv run python migrate_sn.py`
4. **DB 생성**(사내망): data_manager → "운영 현황" 화면 → "수동 작업" → "DB·날짜" 탭
   "DB 생성 실행" + "date.parquet 생성" (또는 헤드리스 `db_job.py` / `date_job.py`)
5. **스케줄 등록** (작업 스케줄러). 소비 앱(tbm·esg)이 더 이상 parquet를 생산하지 않으므로
   생산·가져오기 신선도는 아래 무인 잡이 책임진다. 저장 발생 시 센티널 갱신 → 소비 앱 자동 반영.
   실행은 로컬 venv로: `C:\venvs\data_manager\Scripts\python.exe <job>.py` (시작 위치 F:\code\data_manager).
   네트워크 .venv는 numpy 오류(메모리 [[network-drive-venv-numpy]]).
   - `import_job.py` — **30분마다 권장**. upload/{esg,ptw,out} 신규 파일 자동 변환,
     성공 원본은 `_processed/`로 이동(멱등), 실패는 `_failed/`. 혼재 투입(자동+수동) 대응.
   - `doc_job.py` — 일 1회. upload/{accident,guide} PDF 증분 파싱(이미 처리분 스킵, LLM 절감).
   - `weather_job.py` — 일 2회(06:30/12:00)
   - `db_job.py`(사내망) — 일 1회(예: 07:30). pjtlist/milestone/shipbbs 재생성.
     DB 미연결 시 기존 parquet 보존(덮어쓰지 않음)
   - `date_job.py` — 일 1회(예: 00:30). 범위가 '오늘+7'이라 매일 갱신 필요
   각 잡은 `parquet/_jobstatus/<job>.json`에 결과 기록 → UI "운영 현황"에서 한눈에 확인.
6. **검증**: 아래 C 수행 후 `F:\code\data\tbm\` 삭제

> **UI 변경(재설계 B)**: data_manager 메뉴가 9개 탭 → **2화면**(운영 현황 / 안전메시지)으로 간소화.
> 운영 현황 = 전 parquet 상태표 + 잡 상태 + 수동 작업(fallback) 한 화면. 안전메시지 = 직접 입력
> (tbm admin 이관: 날짜/팀/내용/첨부·링크). 평소 무인 잡이 처리하고 사람은 현황만 확인.

---

## C. 검증 체크리스트

- [ ] `migrate_sn.py` 실행 후 `pjtlist.SHIPNUM`, `milestone.project`, `ptwlist.PJT` 모두 `SN26xx` 형태
- [ ] data_manager ESG/TBM/Out 저장 → `F:\code\data\parquet`에 원자적 생성(.tmp 잔존 없음)
- [ ] ptwlist 업로드 시 `risk_keywords`/`warning` 채워짐, `mapping.parquet` 누적
- [ ] tbm app1/2/3 기동 → ptwlist/milestone/pjtlist 정상 표시, 호선 `SN26xx` **단일** 표기(SNSN 아님)
- [ ] tbm milestone Gantt에 이벤트 12종 표시(read-unpivot 동작)
- [ ] ESG 앱(8502) 기동 → 사내망에서 NAS 인식(로컬 폴백 아님), milestone wide 정상
- [ ] data_manager 저장 후 tbm 새로고침 버튼/재진입 시 최신 반영(센티널)
- [ ] LLM/프록시 차단 상태에서 ptwlist 저장해도 **저장 성공**(빈 위험컬럼), mapping에 빈값 미저장

---

## D. 잔여 수동 작업 (권장)

1. **tbm 생성 메뉴 비활성** — app1_admin의 날씨/매핑/PTW/문서/메시지 생성 버튼은 이제 data_manager로 이관됨.
   혼선·이중정본 방지를 위해 해당 admin view의 생성 버튼을 숨기거나 "data_manager에서 수행" 안내로 교체 권장.
   (현재는 동작은 하나, 운영상 data_manager만 사용할 것)
2. **`F:\code\tbm_system_v6\data1`** (구 로컬 데이터) — 통합 후 미사용. 검증 완료 후 정리.
3. **`check_data_sources.py`** — input/ → parquet/ 경로로 진단 문구 갱신(선택).
4. **`.streamlit/secrets.toml`** (tbm·esg) — `.env` 이관 완료 후 제거 가능(현재 코드가 .env 우선).

# 사내 앱 제작 공통사항

조선소 시운전팀 3개 Streamlit 앱(**data_manager** 생산 / **tbm**·**esg** 소비)의 사내·사외망
공통 규칙. 사내망 배포·디버깅에서 **실제로 확정된 사실** 기준 정리(틀렸던 내용은 교정 반영).

> 단일 소스: `secret/.env` + `app_config.py`(3 repo 동봉, 동일본 유지). 코드 경로는 data_manager 기준.

---

## 0. 한눈에 — 사내망 가동 체크리스트
1. **로컬 venv**: `C:\venvs\<app>` (네트워크 드라이브 금지 — numpy ImportError).
2. **uv 설치**: `UV_NATIVE_TLS=true` + 프록시 + `UV_CONCURRENT_DOWNLOADS=1` + `UV_HTTP_TIMEOUT=300`.
3. **`secret/.env`** 작성(`.env.template` 복사) — NAS/DB/LLM/프록시/기상청 키.
4. `setup_env.bat` 더블클릭(최초) → 이후 `run.bat`.
5. 데이터 생산은 **data_manager만**(tbm·esg는 읽기 전용).

---

## 1. 망 자동감지 (사내/사외)
- 기준: **`NAS_BASE_PATH`가 존재하면 사내망** (`app_config.py:48-50` `IS_INTERNAL`).
- 사내/사외 동작 차이:

| 항목 | 사내망 | 사외망 |
|---|---|---|
| BASE_PATH | `NAS_BASE_PATH` | `DATA_PATH`(기본 `F:\code\data`) |
| PARQUET/UPLOAD | `NAS_*PATH` 또는 BASE 하위 | BASE 하위 |
| 프록시 | ON | OFF(환경변수 제거) |
| LLM | SOLA(사내) | Upstage(사외) |
| DB | 직결 | parquet 폴백 |

## 2. 프록시 정책 (가장 헷갈리는 부분)
- 사내망 ON / **사외망은 환경변수 자체를 제거**(오적용 방지) — `app_config.py:109-116`.
- **내부 LLM(SOLA)은 프록시 우회**: `proxies={"http":None,"https":None}` (`llm_client.py`). 사내 LLM은
  내부망 서버라 인터넷 프록시를 타면 안 됨.
- **외부 API(기상청)는 프록시 사용**: `proxies=secrets.proxies` (`api_weather.py`).
- 규칙: **"인터넷 나가는 호출만 프록시, 사내 서버 호출은 직접."**

## 3. SSL / 인증서
- 런타임: `SSL_VERIFY = not USE_PROXY` → 사내망(프록시 SSL 검사)에서는 `verify=False`.
- **uv 패키지 설치**: 사내 SSL 검사 CA를 신뢰해야 함 → **`UV_NATIVE_TLS=true`**(Windows 인증서 저장소 사용).
  - ⚠️ **`--system-certs`는 uv에 없는 옵션** → `error: unexpected argument '--system-certs' found`.
    반드시 `UV_NATIVE_TLS=true`(환경변수) 또는 `--native-tls`(플래그).
  - 그래도 실패 시: 사내 root CA `.pem` → `SSL_CERT_FILE=C:\path\corp-ca.pem`.

## 4. LLM 호출 규약
- 폴백 순서: **SOLA(사내) → Upstage → OpenAI** (`llm_client.call_llm`).
- **SOLA 요청 본문 함정**: 키 이름이 **`max_token`(단수)** 임. `max_tokens`(복수)면 서버가 HTTP 200 +
  `"please check parameter"`로 거부 → 응답이 LLM 답이 아니라 에러 문자열이라 조용히 실패.
  - 그 외: `authKey`(본문 포함), `number_model`(모델 번호, 예 `"9"`), `generate_kwargs{temperature,max_token,top_p}`.
- 응답은 JSON 기대 → `<think>` 제거 → ```json 펜스 제거 → `json.loads`.

## 5. 외부 API 호출 — 기상청 (날씨)
두 시스템이 **별개**이고 키·URL은 **같은 시스템 짝**이어야 함:

| 시스템 | URL | 키 파라미터 | 키 발급 |
|---|---|---|---|
| 기상청 API허브 | `apihub.kma.go.kr/api/typ02/openApi/VilageFcstInfoService_2.0` | `authKey` | apihub.kma.go.kr(활용신청 필요) |
| 공공데이터포털 | `apis.data.go.kr/1360000/VilageFcstInfoService_2.0` | `serviceKey` | data.go.kr(자동승인, **권장**) |

- 코드는 **`authKey`+`serviceKey` 동시 전송**(`api_weather.py`) → **URL만 맞추면 양쪽 호환**(상대 무시).
- **Decoding 키 사용**: 코드가 `params=`로 전송하며 자동 URL-인코딩 → 원본(Decoding) 키여야 함.
  Encoding 키(`%2B` 등 `%` 포함)를 넣으면 **이중 인코딩 → 401**.
- 응답 구조 `response.body.items.item`(category/fcstValue) — 두 시스템 동일.
- 증상 디버깅: 401 = 키/URL 불일치. 본문에 `SERVICE_KEY_IS_NOT_REGISTERED` = 활용신청 미승인.

## 6. DB 연결
- `USE_PARQUET`/`db_url()` (`app_config.py:71-79`): 사내망 + HOST 설정 시만 MySQL 직결, 그 외 **parquet 폴백**.
- 사내망에서만 DB 생산(data_manager). 소비 앱은 parquet → data2 더미 폴백.

## 7. 경로·시크릿 (실수 잦은 부분)
- **`secret/.env` 단일 소스**. `app_config`가 자기 위치에서 위로 올라가며 `secret/.env` 탐색
  (환경변수 `SHI_ENV_FILE`로 강제 지정 가능).
- **`NAS_PARQUET_PATH`/`NAS_UPLOAD_PATH`는 비우는 게 안전** → BASE_PATH 하위로 자동 조립.
  값을 잘못(또는 예시 `<BASE_PATH>` 꺾쇠째) 넣으면 "경로 없음"으로 깨짐.
- **`.env`가 app_config 기본값을 override** — 특히 `WEATHER_BASE_URL`. 코드만 고쳐도 `.env`에 옛 값이
  있으면 그게 우선.
- **`load_dotenv(override=False)`** — OS 환경변수가 이미 있으면 `.env`를 **덮어쓰지 않음**. "키를 바꿨는데
  안 바뀐다"면 셸/시스템 환경변수가 가리는지 확인(`Remove-Item Env:\KEY`).

## 8. uv 사내망 배포 3종 세트
```powershell
$env:HTTP_PROXY="http://<사내프록시>:포트"; $env:HTTPS_PROXY=$env:HTTP_PROXY
$env:UV_NATIVE_TLS="true"            # 인증서(UnknownIssuer 해결)
$env:UV_CONCURRENT_DOWNLOADS="1"     # 대용량 병렬 다운로드 끊김 방지(직렬화)
$env:UV_HTTP_TIMEOUT="300"
$env:UV_PYTHON_PREFERENCE="only-system"   # 공용PC 32비트 Python 회피, 깨진 관리형 다운로드 차단(9-1 참조)
uv python find cpython-3.12-windows-x86_64   # 64비트 3.12 존재 확인(없으면 설치 안내). 있으면:
uv sync --python cpython-3.12-windows-x86_64   # 64비트 명시(32비트 배제). 끊기면 재실행
```
- 다운로드가 **매번 다른 패키지**에서 실패 = 프록시 throttling → `UV_CONCURRENT_DOWNLOADS=1`이 핵심.

## 9. 네트워크 드라이브 numpy 문제
- F:(매핑 네트워크 드라이브/UNC)에서 venv 실행 시 numpy ImportError → **반드시 `C:\venvs\<app>` 로컬 venv**.
- `check_env.py`가 python 경로가 UNC면 FAIL로 경고.

## 9-1. Python 3.12(64비트) 고정 — C 확장 소스빌드/ MSVC 에러 방지 (중요)
- **증상**: 공용PC `setup_env.bat`에서 `httptools`(또는 pyarrow 등 C 확장)를 **소스 컴파일**하다
  `error: Microsoft Visual C++ 14.0 or greater is required`로 실패(data_manager·ocr·esg 공통).
- **원인**(실측 확정): 공용PC에 **32비트 Python**(`...\Python312-32\python.exe`)이 깔려 있고, `uv sync`가
  Python을 고정하지 않으면 그 **32비트**를 잡음. pandas/httptools 등은 **win32(32비트) 휠이 없어 소스 빌드**
  → MSVC 필요 → 실패. (win_amd64(64비트) 휠은 존재 → 휠 부재가 아니라 **32비트 인터프리터** 문제.)
- **함정**: `uv python install 3.12`(관리형)은 이 uv 버전에서 `Missing expected target directory` 링크 버그로
  **깨질 수 있음** → `--python 3.12`가 다시 32비트 시스템으로 폴백. 그래서 **관리형에 의존하지 말 것**.
- **해결**: **64비트 시스템 Python 3.12를 아키텍처로 명시**해 32비트를 배제 + 없으면 설치 안내(자동 다운로드/빌드 금지).
  - 각 repo 루트 `.python-version` = `3.12`.
  - `setup_env.bat`(pyproject 분기 앞): `set UV_PYTHON_PREFERENCE=only-system` + 사전체크
    `uv python find cpython-3.12-windows-x86_64` → errorlevel 1이면 `[ERROR] install 64-bit` 안내 후 `goto :end`.
  - install: `uv sync --python cpython-3.12-windows-x86_64`.
    (`-x86_64` 요청이 32비트 `...-32` 자동 배제, `only-system`이 깨진 관리형 다운로드 자체를 차단)
  - **전제**: 공용PC에 **64비트 Python 3.12** 설치돼 있어야 함. 없으면 안내문구 표시 →
    python.org "Windows installer (64-bit)" 3.12 설치 후 `setup_env.bat` 재실행.
  - ⚠️ bat 블록 안 echo의 괄호는 `^( ^)` 이스케이프(미이스케이프 시 `)`가 블록 조기종료 → 창 닫힘).
    단, **따옴표 안 괄호는 ^가 리터럴로 출력**되니 괄호/따옴표를 빼고 쓸 것.
- **진단 1줄**(공용PC): `python -c "import platform;print(platform.architecture(),platform.python_version())"`
  → `('32bit', ...)` 이면 본 원인 확정 → 64비트 설치 필요.

## 10. 배치 파일(.bat) 작성
- **ASCII 전용 + CRLF**. 한글 주석을 넣으면 cmd 인코딩(CP949) 충돌로 줄이 깨져 **모든 줄을 명령으로
  오실행**(증상: `@echo off`도 안 먹고 명령 앞글자 잘림). `.gitattributes`에 `*.bat -text`로 CRLF 고정.

---

## 부록 — 포트/엔트리
- data_manager: `app.py` (8510, 생산·대시보드)
- tbm: `app2_tbm.py` (8502, 단일 통합앱 4메뉴 — 소비)
- esg: `app.py` (8503, 소비) · `api_server.py`(React web 백엔드)
- tbm은 **streamlit 1.35.x 고정**(상위 버전 `st.radio` 오류).

## 부록 — app_config.py 동기화
3 repo에 동봉된 `app_config.py`는 **동일본** 유지(로직 수정 시 data_manager 기준 복사). 현재 esg/tbm은
`WEATHER_BASE_URL` 기본값이 구(data.go.kr)일 수 있음 — `.env`로 제어하므로 기능 영향은 없으나 동기화 권장.
